

#### What is a Loss Function?
Loss Function is a method of evaluating how well your algorithm is modelling your dataset
it is basically a mechanism which tells ki humara algorithm kitna accha kaam kar rha haiis 

if the output of loss function is large, it means the algorithm is performing poorly and vice verasa

its a mathematical fuction
loss function is a function of machine learning parameters just like a mathematical function is of certain vaiables

---
#### Why is Loss Function Important?

`You can't improve what you can't measure` - Peter Drucker

Lets take some example of machine learning to understand Loss Functions

in linear regression, while calculating the loss function of a line drawn, we make changes in the slope and the intercept in an attempt to minimise the value of the loss function (Insert some visual over here explaining what i am trynna say oiver here)


now lets understand loss functions in deep learning

CGPA | IQ | Packalge (LPM)
7.1 | 8.3 | 3.2
8.5 | 9.1 | 4.5
6.3 | 10.2 | 6.1
5.1 | 8.7 | 2.7 and so on...

`insert the visual of a neural network over here having 2 input nodes: CGPA and IQ and then 3 nodes in the hidden layer with the output node being the package that the student ends up getitng`

intially we start with some random values for weights and biases
and then using forward propagation, we try to predict the output for a specific students, sy the first one

loss functioun: mean squared error
say for 1st studnet, we get 3.7 as oiutput from our neural network and then we calcualkte the loss using loss function which is mean squared error

and now we change the values using the gradient descent algorithm

now rinse and repeat for all other students and calculate values of weights and biases for each and eveyr single student

and the goal is to minimise the value coming out of the loss function


`make a flowchart of the below`
Loss Functions in deep learing:
regression -> mean squared error, mean absolute erorr, huber loss
classification -> binary cross entropy, categorical cross entropy, hinge Loss (SVM)
autoencoders -> KL divergence, 
GAN -> discriminator loss, minmax GAN loss
object detection -> focal loss
embeddings -> triplet loss

we can even implement loss functions of our own and each function has its own advantage and disadvantage

---

#### Loss Function vs Cost Function

![[Pasted image 20260122144010.png]]

taking the same problem of given CGPA and IQ, predict their packages

`Loss Function is calcilated on a single training example`

single -> loss function., also called error function sometimes

entire batch / training data at once => cost function

---

1. Mean Squared Error

used widely in linear regression and very common in machine learningt
also caled squared loss and L2 Loss

![[Pasted image 20260122144419.png]]

Y_i.  - Y_i hat. whole squared this is the loss function

the cost function:
 1/2 sigma(loss function per row).   [add the relvatn formulae over here]
true - prediction whole squared

why do we need to square? we can just simply calculate the difference
this is because true - predicted returns some values as positive and some values as negative

1 unit -> 1 unit
2 units -> 4 units
3 units  -> 9 units

this means that the value how much away from the true line, the error gets magninfied toi that extent because of the error

the points closer to the line have lower impact relative to the points far far away from the line


because of the quadratic nature, the points farther away are being punished whilke the poitns closer to the line are dealt in linient fashion


make a table of the advantages and disadvantes
advantages:
1. easy to interpret
2. the loss function is always differentiable thanks to the equation
3. 1 local minima. it means there is a value of weights and biases where the value of the loss function is minimum
distadvantages:
4. the unit in which error is calculated is unit squared
5. it is not robust to outliers, so if our data has outliers, mean squared error is not a reliable loss function to be used

in context of deep learning, if we have to apply mean square error, the activation function of the last node/ output node must be linear.  this is a very important condition. highlight this one specially


---
2. Mean Absolute Error (MAE)
also known as L1

formula = y_i - Y_i hat absolute value (integrate 2 mods in here)

cost function for n points

we have just replaced the square with absolute value

most shit is same as mean squared error

Advantages:
1. intuitive and easy to understand
2. unit same as y. (the problem os squared units faced in mean squared error is fixed)
3. robust to outliers. (another problem solved). hence should be used when outliers in the data
Disadvantages:
4. not diffferentiable  which means we cannot directly apply gradient descent, we need to calculate sub gradient descents over here, which leads to higher computational complexity


---

3. Huber Loss

imageine a scenario where in the dataset, 25% of the points that we have are outliers
in that case, either of MAE or MSE fails to model our data correctly

MSE will try to come to 25% whereas MAE will treat all them as equal

formula:

L = 1/2(y-ycap) squared given data is not outlier

if the data is outlier, delta (y-yi) mmodulus - 1/2 delta squared


baically a combination of mean squared error and mean absolute error

it lies between MAE and MSE and it performs the best when our data has a lot of outliers

---

4. Binary Cross Entropy / Log Loss Function
used in a classification setup

used in logistic regression
used only when we have 2 classes as in yes and no as suggested in the name binary


formula:   -y log ycap - (1-y) log(1-ycap)

y is the taget valuie and y hat is the prediction of the neural network

 but the final layer/ outper layer activation function has to be sigmoid in every case


cost function:  = 1/n  [sigma ] include the formula over here

remember used in a 2 class classification problem and make sure that the activation functionin the output layer is sigmoid

advantage:

1. the loss function is differentiable so we can easily apply gradient descent on top of it

disadvantage:
1. there can be multiple local minimas 
2. not very inttuitive


---

5. categorical cross entropy

![[Pasted image 20260122150545.png]]



used in classification scenario when we have a multi class classification problem

used in softmax regression as well (refer to machine learning shit to know more)


for a single points here is the formula

summation of j-=1 to K. yj log *yj cap*


where k is the number of classes in the data
for there classes, expand it once (just for illustrative purposes)


whenever we are working on a multi class classificaiton problem, we put as many neurons in the output layer as we have the number of classes that we have to classify our data in

the output layer neurons' activation function has to be softmax
include the mathematical formula for softmax

for both yes and no change the vlaues of z in the softmax formula


the numbers are from 0 to 1 and when we add all of them, we get 1



how does it work?

we have to one hot encode all the classes in the dataset
and then explain how using forward propagartionthis is implemented

---

Sparese Categorical Cross Entropy

the difference between this oine and the original categorical cross entropy

we import our data as integer in sparse categorical entriopy\

thats the only difference
there is also a difference in how the loss is calculated (mention this what vaues in the formula are considered and which ones are omitted) only the first log is calculated and the rest twoi are omiited

we also dont need to do one hot encoding in this one

basically they are lamost same

but we just make a few assumnptions in this one and its preferered when we hafe too many clsses
include the formula for cost function of each one above as well




